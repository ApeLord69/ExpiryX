<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    android:fitsSystemWindows="true">

    <!-- Top bar (always visible) -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp"
        android:gravity="center_vertical"
        android:background="@drawable/header_background"
        android:elevation="2dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/titleHistory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="History"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="?attr/colorOnBackground" />

        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/btnFilter"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_filter"
            android:contentDescription="Filter"
            android:layout_marginEnd="16dp"
            android:tint="?attr/colorOnSurface" />

        <ImageView
            android:id="@+id/btnSearch"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_search_unfilled"
            android:contentDescription="Search"
            android:tint="?attr/colorOnSurface" />


    </LinearLayout>

    <View
        android:id="@+id/topSeparator"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@drawable/nav_separator"
        android:layout_below="@id/topBar" />

    <!-- Search bar (appears below top bar) -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchViewHistory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="Search history..."
        android:iconifiedByDefault="false"
        android:visibility="gone"
        android:background="@drawable/edit_text_background"
        android:padding="8dp"
        android:layout_below="@id/topSeparator" />

    <!-- Counters (below search view) -->
    <LinearLayout
        android:id="@+id/countersLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingTop="10dp"
        android:paddingBottom="6dp"
        android:layout_below="@id/topSeparator"
        android:gravity="center">

        <TextView
            android:id="@+id/textExpiredCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 Expired"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/pill_expired_bg" />

        <TextView
            android:id="@+id/textConsumedCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 Consumed"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/pill_used_bg" />

        <TextView
            android:id="@+id/textDeletedCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 Deleted"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:background="@drawable/pill_deleted_bg" />
    </LinearLayout>


    <!-- Sort dropdown row -->
    <include
        android:id="@+id/layoutSortHistory"
        layout="@layout/sort_history_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/countersLayout" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerHistory"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layoutSortHistory"
        android:layout_above="@id/bottomNav"
        android:clipToPadding="false"
        android:paddingHorizontal="8dp"
        android:paddingBottom="8dp"
        android:visibility="gone" />

    <!-- Placeholder -->
    <LinearLayout
        android:id="@+id/historyPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layoutSortHistory"
        android:layout_above="@id/bottomNav"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_clock_unfilled"
            android:contentDescription="History icon"
            android:alpha="0.5" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Your usage history will appear here."
            android:textSize="16sp"
            android:textColor="?attr/colorOnBackground" />
    </LinearLayout>

    <!-- No Results Placeholder -->
    <LinearLayout
        android:id="@+id/historyNoResults"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layoutSortHistory"
        android:layout_above="@id/bottomNav"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_search_unfilled"
            android:contentDescription="No results"
            android:alpha="0.5" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="No matching history found."
            android:textSize="16sp"
            android:textColor="?attr/colorOnBackground" />
    </LinearLayout>

    <!-- No Favourites Placeholder -->
    <LinearLayout
        android:id="@+id/historyNoFavourites"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layoutSortHistory"
        android:layout_above="@id/bottomNav"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_heart_unfilled"
            android:contentDescription="No favourites"
            android:alpha="0.5" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="No favourite history items yet."
            android:textSize="16sp"
            android:textColor="?attr/colorOnBackground" />
    </LinearLayout>


    <include
        android:id="@+id/bottomNav"
        layout="@layout/include_bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true" />
</RelativeLayout>
